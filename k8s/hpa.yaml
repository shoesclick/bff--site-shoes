apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bff--site-shoes-hpa
  namespace: shoes-eks
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bff--site-shoes-deployment
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
  